Clinic-aMappingToAStoreDecidesNoRole
  1 types analysed
    1 UNCLASSIFIED
      1 UNKNOWN
  0 declared, 1 inferred, 0 left ambiguous
  com.acme.clinic.vet.Speciality: UNCLASSIFIED (LOW, inferred)
    UNKNOWN: nothing in the perimeter uses it, so there was no context to read it in
    to make it explicit: com.acme.clinic.vet.Speciality is used nowhere the analysis can see: declare its kind in the classification configuration, or give it the context that would settle it
    derivation:
      no signal about com.acme.clinic.vet.Speciality

Clinic-aStoreOnlyTheAdapterTouchesStaysOutsideTheDomain
  2 types analysed
    1 DRIVEN_ADAPTER
    1 UNCLASSIFIED
      1 UNKNOWN
  0 declared, 2 inferred, 0 left ambiguous
  com.acme.clinic.billing.InvoiceLedger: DRIVEN_ADAPTER (HIGH, inferred)
    [framework knowledge] com.acme.clinic.billing.InvoiceLedger is a DRIVEN_ADAPTER because it holds JdbcTemplate, which reaches outside the application (org.springframework.jdbc.core.JdbcTemplate)
      involving org.springframework.jdbc.core.JdbcTemplate
    derivation:
      [DECISION] DRIVEN_ADAPTER(com.acme.clinic.billing.InvoiceLedger) [decided on 1 signal of framework knowledge]
        [W1-DR] DRIVEN_ADAPTER(com.acme.clinic.billing.InvoiceLedger) [framework knowledge: INFRA_DEPENDENCY(org.springframework.jdbc.core.JdbcTemplate)]
          INFRA_DEPENDENCY(org.springframework.jdbc.core.JdbcTemplate) [spring:org.springframework.jdbc.core.JdbcTemplate]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      W1-DR: reads a type that holds a tool reaching outside the hexagon as the driven adapter it is
  com.acme.clinic.billing.InvoiceRow: UNCLASSIFIED (LOW, inferred)
    UNKNOWN: nothing in the perimeter uses it, so there was no context to read it in
    to make it explicit: com.acme.clinic.billing.InvoiceRow is used nowhere the analysis can see: declare its kind in the classification configuration, or give it the context that would settle it
    derivation:
      no signal about com.acme.clinic.billing.InvoiceRow

Clinic-theListenerIsAnEntryPointWhereverItIsWritten
  1 types analysed
    1 DRIVING_ADAPTER
  0 declared, 1 inferred, 0 left ambiguous
  com.acme.clinic.schedule.VisitReminders: DRIVING_ADAPTER (HIGH, inferred)
    [framework knowledge] com.acme.clinic.schedule.VisitReminders is a DRIVING_ADAPTER because the framework calls it from outside the application (org.springframework.kafka.annotation.KafkaListener)
    derivation:
      [DECISION] DRIVING_ADAPTER(com.acme.clinic.schedule.VisitReminders) [decided on 1 signal of framework knowledge]
        [W1-DA] DRIVING_ADAPTER(com.acme.clinic.schedule.VisitReminders) [framework knowledge: DRIVING_ENTRYPOINT(com.acme.clinic.schedule.VisitReminders)]
          DRIVING_ENTRYPOINT(com.acme.clinic.schedule.VisitReminders) [spring:org.springframework.kafka.annotation.KafkaListener]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      W1-DA: reads a type the framework calls from outside as the driving adapter it is

Clinic-theServiceBetweenTheControllerAndTheRepository
  4 types analysed
    1 AGGREGATE_ROOT
    1 DRIVEN_PORT
    1 APPLICATION_SERVICE
    1 DRIVING_ADAPTER
  0 declared, 4 inferred, 0 left ambiguous
  com.acme.clinic.owner.Owner: AGGREGATE_ROOT (HIGH, inferred)
    signals, strongest first: declared intent > framework knowledge > graph relation > local structure > topology > naming
    [framework knowledge] com.acme.clinic.owner.Owner is a AGGREGATE_ROOT because a repository stores and retrieves it (org.springframework.data.repository.Repository)
      involving com.acme.clinic.owner.OwnerRepository
    [graph relation] com.acme.clinic.owner.Owner is a AGGREGATE_ROOT because OwnerRepository keeps it and hands it back, which is a lifecycle of its own
      involving com.acme.clinic.owner.OwnerRepository
    derivation:
      [DECISION] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [decided on 1 signal of framework knowledge, 1 of graph relation]
        [R1] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [framework knowledge: SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository)]
          SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository) [spring:org.springframework.data.repository.Repository]
        [R1b] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [graph relation: MANAGED_BY(com.acme.clinic.owner.OwnerRepository)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R1: reads a Spring Data repository declaration for everything it says
      R1b: reads the type a way out stores and retrieves as the aggregate it is
  com.acme.clinic.owner.OwnerController: DRIVING_ADAPTER (HIGH, inferred)
    [framework knowledge] com.acme.clinic.owner.OwnerController is a DRIVING_ADAPTER because the framework calls it from outside the application (org.springframework.web.bind.annotation.RestController)
    derivation:
      [DECISION] DRIVING_ADAPTER(com.acme.clinic.owner.OwnerController) [decided on 1 signal of framework knowledge]
        [W1-DA] DRIVING_ADAPTER(com.acme.clinic.owner.OwnerController) [framework knowledge: DRIVING_ENTRYPOINT(com.acme.clinic.owner.OwnerController)]
          DRIVING_ENTRYPOINT(com.acme.clinic.owner.OwnerController) [spring:org.springframework.web.bind.annotation.RestController]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      W1-DA: reads a type the framework calls from outside as the driving adapter it is
  com.acme.clinic.owner.OwnerRepository: DRIVEN_PORT (HIGH, inferred)
    signals, strongest first: declared intent > framework knowledge > graph relation > local structure > topology > naming
    [framework knowledge] com.acme.clinic.owner.OwnerRepository is a DRIVEN_PORT because it is a Spring Data repository (org.springframework.data.repository.Repository)
    [graph relation] com.acme.clinic.owner.OwnerRepository is a DRIVEN_PORT because OwnerService calls it and nothing inside the hexagon implements it
      involving com.acme.clinic.owner.OwnerService
    derivation:
      [DECISION] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [decided on 1 signal of framework knowledge, 1 of graph relation]
        [R1] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [framework knowledge: SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository)]
          SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository) [spring:org.springframework.data.repository.Repository]
        [R4] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [graph relation: CONSUMED_BY_CORE(com.acme.clinic.owner.OwnerService)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R1: reads a Spring Data repository declaration for everything it says
      R4: reads a contract the core calls and nothing inside fulfils as the driven port it is
  com.acme.clinic.owner.OwnerService: APPLICATION_SERVICE (HIGH, inferred)
    [graph relation] com.acme.clinic.owner.OwnerService is a APPLICATION_SERVICE because it stands on OwnerRepository, through which the application reaches outside
      involving com.acme.clinic.owner.OwnerRepository
    [graph relation] com.acme.clinic.owner.OwnerService is a APPLICATION_SERVICE because OwnerController hands the work to it, and it reaches outside through OwnerRepository
      involving com.acme.clinic.owner.OwnerController
    derivation:
      [DECISION] APPLICATION_SERVICE(com.acme.clinic.owner.OwnerService) [decided on 2 signals of graph relation]
        [R6] APPLICATION_SERVICE(com.acme.clinic.owner.OwnerService) [graph relation: CALLS_DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository)]
        [R6b] APPLICATION_SERVICE(com.acme.clinic.owner.OwnerService) [graph relation: REACHED_FROM_THE_RING(com.acme.clinic.owner.OwnerController)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R6: reads the type standing between the ports as the application service it is
      R6b: reads the type an entry point delegates to as the application service it is

Clinic-theWholeClinic
  7 types analysed
    1 AGGREGATE_ROOT
    1 VALUE_OBJECT
    2 DRIVEN_PORT
    1 APPLICATION_SERVICE
    1 DRIVING_ADAPTER
    1 DRIVEN_ADAPTER
  0 declared, 7 inferred, 0 left ambiguous
  com.acme.clinic.notification.EmailVisitNotifier: DRIVEN_ADAPTER (HIGH, inferred)
    signals, strongest first: declared intent > framework knowledge > graph relation > local structure > topology > naming
    [framework knowledge] com.acme.clinic.notification.EmailVisitNotifier is a DRIVEN_ADAPTER because it holds RestTemplate, which reaches outside the application (org.springframework.web.client.RestTemplate)
      involving org.springframework.web.client.RestTemplate
    [graph relation] com.acme.clinic.notification.EmailVisitNotifier is a DRIVEN_ADAPTER because it answers VisitNotifier, which the hexagon calls to reach outside
      involving com.acme.clinic.notification.VisitNotifier
    derivation:
      [DECISION] DRIVEN_ADAPTER(com.acme.clinic.notification.EmailVisitNotifier) [decided on 1 signal of framework knowledge, 1 of graph relation]
        [W1-DR] DRIVEN_ADAPTER(com.acme.clinic.notification.EmailVisitNotifier) [framework knowledge: INFRA_DEPENDENCY(org.springframework.web.client.RestTemplate)]
          INFRA_DEPENDENCY(org.springframework.web.client.RestTemplate) [spring:org.springframework.web.client.RestTemplate]
        [W1-DR2] DRIVEN_ADAPTER(com.acme.clinic.notification.EmailVisitNotifier) [graph relation: IMPLEMENTS_DRIVEN_PORT(com.acme.clinic.notification.VisitNotifier)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      W1-DR: reads a type that holds a tool reaching outside the hexagon as the driven adapter it is
      W1-DR2: reads what fulfils an established way out as the driven adapter it is
  com.acme.clinic.notification.VisitNotifier: DRIVEN_PORT (HIGH, inferred)
    [graph relation] com.acme.clinic.notification.VisitNotifier is a DRIVEN_PORT because OwnerService calls it and nothing inside the hexagon implements it
      involving com.acme.clinic.owner.OwnerService
    derivation:
      [DECISION] DRIVEN_PORT(com.acme.clinic.notification.VisitNotifier) [decided on 1 signal of graph relation]
        [R4] DRIVEN_PORT(com.acme.clinic.notification.VisitNotifier) [graph relation: CONSUMED_BY_CORE(com.acme.clinic.owner.OwnerService)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R4: reads a contract the core calls and nothing inside fulfils as the driven port it is
  com.acme.clinic.owner.Owner: AGGREGATE_ROOT (HIGH, inferred)
    signals, strongest first: declared intent > framework knowledge > graph relation > local structure > topology > naming
    [framework knowledge] com.acme.clinic.owner.Owner is a AGGREGATE_ROOT because a repository stores and retrieves it (org.springframework.data.repository.Repository)
      involving com.acme.clinic.owner.OwnerRepository
    [graph relation] com.acme.clinic.owner.Owner is a AGGREGATE_ROOT because OwnerRepository keeps it and hands it back, which is a lifecycle of its own
      involving com.acme.clinic.owner.OwnerRepository
    derivation:
      [DECISION] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [decided on 1 signal of framework knowledge, 1 of graph relation]
        [R1] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [framework knowledge: SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository)]
          SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository) [spring:org.springframework.data.repository.Repository]
        [R1b] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [graph relation: MANAGED_BY(com.acme.clinic.owner.OwnerRepository)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R1: reads a Spring Data repository declaration for everything it says
      R1b: reads the type a way out stores and retrieves as the aggregate it is
  com.acme.clinic.owner.OwnerController: DRIVING_ADAPTER (HIGH, inferred)
    [framework knowledge] com.acme.clinic.owner.OwnerController is a DRIVING_ADAPTER because the framework calls it from outside the application (org.springframework.web.bind.annotation.RestController)
    derivation:
      [DECISION] DRIVING_ADAPTER(com.acme.clinic.owner.OwnerController) [decided on 1 signal of framework knowledge]
        [W1-DA] DRIVING_ADAPTER(com.acme.clinic.owner.OwnerController) [framework knowledge: DRIVING_ENTRYPOINT(com.acme.clinic.owner.OwnerController)]
          DRIVING_ENTRYPOINT(com.acme.clinic.owner.OwnerController) [spring:org.springframework.web.bind.annotation.RestController]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      W1-DA: reads a type the framework calls from outside as the driving adapter it is
  com.acme.clinic.owner.OwnerRepository: DRIVEN_PORT (HIGH, inferred)
    signals, strongest first: declared intent > framework knowledge > graph relation > local structure > topology > naming
    [framework knowledge] com.acme.clinic.owner.OwnerRepository is a DRIVEN_PORT because it is a Spring Data repository (org.springframework.data.repository.Repository)
    [graph relation] com.acme.clinic.owner.OwnerRepository is a DRIVEN_PORT because OwnerService calls it and nothing inside the hexagon implements it
      involving com.acme.clinic.owner.OwnerService
    derivation:
      [DECISION] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [decided on 1 signal of framework knowledge, 1 of graph relation]
        [R1] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [framework knowledge: SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository)]
          SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository) [spring:org.springframework.data.repository.Repository]
        [R4] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [graph relation: CONSUMED_BY_CORE(com.acme.clinic.owner.OwnerService)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R1: reads a Spring Data repository declaration for everything it says
      R4: reads a contract the core calls and nothing inside fulfils as the driven port it is
  com.acme.clinic.owner.OwnerService: APPLICATION_SERVICE (HIGH, inferred)
    [graph relation] com.acme.clinic.owner.OwnerService is a APPLICATION_SERVICE because it stands on VisitNotifier, through which the application reaches outside
      involving com.acme.clinic.notification.VisitNotifier
    [graph relation] com.acme.clinic.owner.OwnerService is a APPLICATION_SERVICE because OwnerController hands the work to it, and it reaches outside through VisitNotifier
      involving com.acme.clinic.owner.OwnerController
    derivation:
      [DECISION] APPLICATION_SERVICE(com.acme.clinic.owner.OwnerService) [decided on 2 signals of graph relation]
        [R6] APPLICATION_SERVICE(com.acme.clinic.owner.OwnerService) [graph relation: CALLS_DRIVEN_PORT(com.acme.clinic.notification.VisitNotifier)]
        [R6b] APPLICATION_SERVICE(com.acme.clinic.owner.OwnerService) [graph relation: REACHED_FROM_THE_RING(com.acme.clinic.owner.OwnerController)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R6: reads the type standing between the ports as the application service it is
      R6b: reads the type an entry point delegates to as the application service it is
  com.acme.clinic.owner.Pet: VALUE_OBJECT (HIGH, inferred)
    [graph relation] com.acme.clinic.owner.Pet is a VALUE_OBJECT because Owner is made of it and it carries no identity of its own
      involving com.acme.clinic.owner.Owner
    derivation:
      [DECISION] VALUE_OBJECT(com.acme.clinic.owner.Pet) [decided on 1 signal of graph relation]
        [R3b] VALUE_OBJECT(com.acme.clinic.owner.Pet) [graph relation: OWNED_BY(com.acme.clinic.owner.Owner)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R3b: reads a part of an aggregate that carries no identity as the value it is

Clinic-whatSpringDataManagesIsTheAggregate
  2 types analysed
    1 AGGREGATE_ROOT
    1 DRIVEN_PORT
  0 declared, 2 inferred, 0 left ambiguous
  com.acme.clinic.owner.Owner: AGGREGATE_ROOT (HIGH, inferred)
    signals, strongest first: declared intent > framework knowledge > graph relation > local structure > topology > naming
    [framework knowledge] com.acme.clinic.owner.Owner is a AGGREGATE_ROOT because a repository stores and retrieves it (org.springframework.data.repository.Repository)
      involving com.acme.clinic.owner.OwnerRepository
    [graph relation] com.acme.clinic.owner.Owner is a AGGREGATE_ROOT because OwnerRepository keeps it and hands it back, which is a lifecycle of its own
      involving com.acme.clinic.owner.OwnerRepository
    derivation:
      [DECISION] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [decided on 1 signal of framework knowledge, 1 of graph relation]
        [R1] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [framework knowledge: SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository)]
          SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository) [spring:org.springframework.data.repository.Repository]
        [R1b] AGGREGATE_ROOT(com.acme.clinic.owner.Owner) [graph relation: MANAGED_BY(com.acme.clinic.owner.OwnerRepository)]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R1: reads a Spring Data repository declaration for everything it says
      R1b: reads the type a way out stores and retrieves as the aggregate it is
  com.acme.clinic.owner.OwnerRepository: DRIVEN_PORT (HIGH, inferred)
    [framework knowledge] com.acme.clinic.owner.OwnerRepository is a DRIVEN_PORT because it is a Spring Data repository (org.springframework.data.repository.Repository)
    derivation:
      [DECISION] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [decided on 1 signal of framework knowledge]
        [R1] DRIVEN_PORT(com.acme.clinic.owner.OwnerRepository) [framework knowledge: SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository)]
          SPRING_DATA_REPOSITORY(com.acme.clinic.owner.OwnerRepository) [spring:org.springframework.data.repository.Repository]
    rules cited:
      DECISION: weighs every signal held about a type and commits to one kind
      R1: reads a Spring Data repository declaration for everything it says

