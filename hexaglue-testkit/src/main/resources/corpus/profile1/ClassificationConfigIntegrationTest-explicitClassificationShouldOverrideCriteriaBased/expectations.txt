# ClassificationConfigIntegrationTest#explicitClassificationShouldOverrideCriteriaBased
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result).isPresent()
#   assertThat(result.get().kind()).isEqualTo("VALUE_OBJECT")
#   assertThat(result.get().matchedCriteria()).isEqualTo("ExplicitConfiguration")
#   assertThat(result.get().confidence()).isEqualTo(ConfidenceLevel.HIGH)
#   assertThat(result.get().matchedPriority()).isEqualTo(100)

# Reviewed under the doctrine. The legacy test stated the kind through the tool
# configuration, which is not part of the fixture: harvesting kept the sources and left the
# configuration behind. What the sources carry is all this scenario can be held to, and
# they carry nothing.
status: reviewed
expect: com.example.OrderDetails = UNCLASSIFIED
