# ClassificationConfigIntegrationTest#generatedInterfacesShouldBeSkippedFromPortClassification
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(results.get(NodeId.type("com.example.OrderJpaRepository"))) .isEmpty()

# Reviewed under the doctrine, and this pins a real behaviour: the frontend keeps generated
# code out of the model altogether (AnalysisPerimeter), so the engine never sees the type
# and owes it no verdict. Re-reading what a generator emitted would teach the tool the
# generator's conventions instead of the author's intent.
#
# Worth noting as a divergence: the referential foresees generated code as an UNCLASSIFIED
# carrying the GENERATED_CODE category, and the jakarta pack carries a GENERATED_CODE entry
# that no source type can trigger while the frontend filters first. This scenario states
# what is delivered; which of the two doctrines wins is recorded at the registry.
#
# The hand-written interface next to it is undecided for the ordinary reason: nobody
# implements it and nobody consumes it.
status: reviewed
expect: com.example.OrderJpaRepository = NO VERDICT
expect: com.example.OrderRepository = UNCLASSIFIED
