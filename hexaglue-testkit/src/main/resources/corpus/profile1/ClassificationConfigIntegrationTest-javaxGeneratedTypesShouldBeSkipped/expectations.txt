# ClassificationConfigIntegrationTest#javaxGeneratedTypesShouldBeSkipped
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(results.get(NodeId.type("com.example.Order"))).isPresent()
#   assertThat(results.get(NodeId.type("com.example.OrderMapper"))).isEmpty()

# Reviewed under the doctrine, and this pins a real behaviour: the frontend keeps generated
# code out of the model altogether (AnalysisPerimeter), so the engine never sees the type
# and owes it no verdict. Re-reading what a generator emitted would teach the tool the
# generator's conventions instead of the author's intent.
#
# Worth noting as a divergence: the stated doctrine of the engine foresees generated code
# as an UNCLASSIFIED carrying the GENERATED_CODE category, and the jakarta pack carries such
# an entry, which no source type can trigger while the frontend filters first. It states
# what is delivered; which of the two wins is still open.
status: reviewed
expect: com.example.OrderMapper = NO VERDICT
expect: com.example.Order = UNCLASSIFIED
