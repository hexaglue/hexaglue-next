# ClassificationContractTest#deterministicDecision
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.kind()) .as("Run %d: kind should be identical", run) .isEqualTo(firstResult.kind())
#   assertThat(result.matchedCriteria()) .as("Run %d: criteria should be identical", run) .isEqualTo(firstResult.matchedCriteria())
#   assertThat(result.matchedPriority()) .as("Run %d: priority should be identical", run) .isEqualTo(firstResult.matchedPriority())
#   assertThat(result.confidence()) .as("Run %d: confidence should be identical", run) .isEqualTo(firstResult.confidence())

status: draft
# the assertions name no single subject; decide by hand
