# ClassificationContractTest#priorityDominatesConfidence
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.isClassified()).isTrue()
#   assertThat(result.kind()).isEqualTo("AGGREGATE_ROOT")
#   assertThat(result.matchedPriority()).isEqualTo(100)
#   assertThat(result.matchedCriteria()).isEqualTo("explicit-aggregate-root")
#   assertThat(result.confidence()).isEqualTo(ConfidenceLevel.EXPLICIT)

# Reviewed under the doctrine: the author declares the kind, and a declaration is the
# strongest evidence there is. The legacy verdict said the same thing.
#
# The interface alongside it stays undecided: nobody implements it and nobody consumes
# it, so it holds no position (the anti-rule, again).
status: reviewed
expect: com.example.Order = AGGREGATE_ROOT
expect: com.example.OrderRepository = UNCLASSIFIED
