# ClassificationGoldenFilesTest#createApplicationServiceExample
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   (no assertion found)

# Reviewed under the doctrine: this fixture carries the wiring the rules read, so it states
# what the engine owes once the waves are built. The claims below are what the relations
# say, not what the names suggest.
#
# The service consumes the repository, which makes it a driven port (R4) and makes the
# service the application layer (R6); the port's signatures converge on Order, which makes
# it the aggregate (R1b) and its search key the identity (R2). The inbound interface is a
# way in: the core writes it and nothing inside takes it, so its caller is outside (R5b).
status: reviewed
expect: com.example.order.application.OrderService = APPLICATION_SERVICE
expect: com.example.order.domain.Order = AGGREGATE_ROOT
expect: com.example.order.domain.OrderId = IDENTIFIER
expect: com.example.order.ports.out.OrderRepository = DRIVEN_PORT
expect: com.example.order.ports.in.OrderUseCases = DRIVING_PORT
