# ClassificationIntegrationTest#portsInOutClassification
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(useCaseResult.kind()).isEqualTo(PortKind.USE_CASE.name())
#   assertThat(useCaseResult.confidence()).isEqualTo(ConfidenceLevel.EXPLICIT)
#   assertThat(useCaseResult.portDirection()).isEqualTo(PortDirection.DRIVING)
#   assertThat(repoResult.kind()).isEqualTo(PortKind.REPOSITORY.name())
#   assertThat(repoResult.confidence()).isEqualTo(ConfidenceLevel.EXPLICIT)
#   assertThat(repoResult.portDirection()).isEqualTo(PortDirection.DRIVEN)
#   assertThat(gatewayResult.kind()).isEqualTo(PortKind.GATEWAY.name())
#   assertThat(gatewayResult.confidence()).isEqualTo(ConfidenceLevel.EXPLICIT)
#   assertThat(gatewayResult.portDirection()).isEqualTo(PortDirection.DRIVEN)

# Reviewed under the doctrine: the author declares the port with a jMolecules annotation.
# The legacy engine answered a port family (REPOSITORY, GATEWAY); v7 states the kind and
# carries the family as an attribute of the port - the same statement in another
# vocabulary.
#
# The three ports are declared, and their packages are irrelevant to the verdict - one of
# them sits in infrastructure and is a port all the same. The class nobody manages stays
# undecided.
status: reviewed
expect: com.example.infrastructure.PaymentGateway = DRIVEN_PORT
expect: com.example.ports.out.OrderRepository = DRIVEN_PORT
expect: com.example.ports.in.PlaceOrderUseCase = DRIVING_PORT
expect: com.example.domain.Order = UNCLASSIFIED
