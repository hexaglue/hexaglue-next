# ConflictDetectionTest#conflictingExplicitAnnotationsWithSamePriority
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.status()).isEqualTo(ClassificationStatus.CONFLICT)
#   assertThat(result.isClassified()).isFalse()
#   assertThat(result.hasConflicts()).isTrue()
#   assertThat(conflictKinds.size()).isGreaterThanOrEqualTo(1)

# Reviewed under the doctrine: the author declares two kinds at once, at the same tier and
# the same distance. Nothing separates them, so the engine says so and keeps both as
# candidates rather than picking one. An honest "I could not tell" is what a report can
# act on.
status: reviewed
expect: com.example.Ambiguous = UNCLASSIFIED
