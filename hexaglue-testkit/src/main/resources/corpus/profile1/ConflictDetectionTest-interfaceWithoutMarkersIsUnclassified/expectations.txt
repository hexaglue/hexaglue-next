# ConflictDetectionTest#interfaceWithoutMarkersIsUnclassified
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.isUnclassified()).isTrue()

# Reviewed, and agreeing with the carriere for a reason the carriere never had. An earlier
# reading answered DRIVEN_PORT off the conventional name; naming is no longer a default
# posture, and nothing else here speaks. The fixture is one interface nobody implements,
# calls or injects: a role is a position in a graph, and this type has no position. The
# package says ports.in, which is a word in a name too, and would in any case contradict a
# driven port rather than establish one.
#
# This is the anti-rule the corpus exists to pin: not every interface is a port. Widen the
# perimeter to the code that uses it, or declare it, and the verdict follows.
status: reviewed
expect: com.example.ports.in.CustomerRepository = UNCLASSIFIED
