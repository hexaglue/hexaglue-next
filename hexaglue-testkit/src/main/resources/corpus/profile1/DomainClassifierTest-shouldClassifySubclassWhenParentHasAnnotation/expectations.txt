# DomainClassifierTest#shouldClassifySubclassWhenParentHasAnnotation
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(parentResult.isClassified()).isTrue()
#   assertThat(parentResult.kind()).isEqualTo("AGGREGATE_ROOT")
#   assertThat(parentResult.matchedCriteria()).isEqualTo("explicit-aggregate-root")
#   assertThat(childResult.isClassified()) .as("Subclass should inherit AGGREGATE_ROOT classification from annotated parent") .isTrue()
#   assertThat(childResult.kind()).isEqualTo("AGGREGATE_ROOT")
#   assertThat(childResult.matchedCriteria()).isEqualTo("inherited-classification")

status: draft
# the assertions name no single subject; decide by hand
