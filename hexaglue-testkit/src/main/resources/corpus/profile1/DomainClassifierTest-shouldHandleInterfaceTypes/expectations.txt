# DomainClassifierTest#shouldHandleInterfaceTypes
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.isUnclassified()).isTrue()

# Reviewed, and agreeing with the carriere for a reason the carriere never had, exactly as in
# ConflictDetectionTest-interfaceWithoutMarkersIsUnclassified: an isolated interface holds no
# position in any graph, and its name is not a substitute for one. An application service is
# recognized by sitting between a driving port and the driven ports it consumes; this type
# sits between nothing.
status: reviewed
expect: com.example.OrderService = UNCLASSIFIED
