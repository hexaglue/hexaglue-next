# DomainCriteriaTest#shouldMatchRecordWithSingleComponentAndIdSuffix
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isTrue()
#   assertThat(result.confidence()).isEqualTo(ConfidenceLevel.HIGH)
#   assertThat(result.justification()).contains("OrderId")
#   assertThat(criteria.targetKind()).isEqualTo(ElementKind.IDENTIFIER)
#   assertThat(criteria.priority()).isEqualTo(80)

# Reviewed under the doctrine: an immutable single-value wrapper is as much a value object
# as an identity, and nothing in this fixture separates the two. Both readings are kept as
# candidates and the verdict says so instead of picking. What would separate them is use -
# an identity is the search key of a port managing an aggregate - and there is no port
# here.
status: reviewed
expect: com.example.OrderId = UNCLASSIFIED
