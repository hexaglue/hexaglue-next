# DomainCriteriaTest#shouldMatchRecordWithoutIdentityReferencedByOtherType
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isTrue()
#   assertThat(result.confidence()).isEqualTo(ConfidenceLevel.MEDIUM)
#   assertThat(result.justification()).contains("ShippingRate")
#   assertThat(criteria.targetKind()).isEqualTo(ElementKind.VALUE_OBJECT)
#   assertThat(criteria.priority()).isEqualTo(65)

# Reviewed under the doctrine: the shape decides, and it decides alone. A type whose state
# cannot change after construction is a value, whatever it is called and whoever uses it.
status: reviewed
expect: com.example.Order = UNCLASSIFIED
expect: com.example.ShippingRate = VALUE_OBJECT
