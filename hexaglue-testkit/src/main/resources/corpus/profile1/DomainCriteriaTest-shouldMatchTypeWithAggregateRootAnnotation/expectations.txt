# DomainCriteriaTest#shouldMatchTypeWithAggregateRootAnnotation
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isTrue()
#   assertThat(result.confidence()).isEqualTo(ConfidenceLevel.EXPLICIT)
#   assertThat(result.justification()).contains("@AggregateRoot")
#   assertThat(criteria.targetKind()).isEqualTo(ElementKind.AGGREGATE_ROOT)
#   assertThat(criteria.priority()).isEqualTo(100)

# Reviewed: the author declares the kind with a jMolecules annotation, named in full.
# Declared intent (S1), EXPLICIT. The legacy verdict said the same thing.
status: reviewed
expect: com.example.Order = AGGREGATE_ROOT
