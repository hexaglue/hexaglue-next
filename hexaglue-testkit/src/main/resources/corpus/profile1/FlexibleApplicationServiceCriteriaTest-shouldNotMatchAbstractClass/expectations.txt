# FlexibleApplicationServiceCriteriaTest#shouldNotMatchAbstractClass
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isFalse()

# Reviewed under the doctrine: an adapter calling the interface would be the strongest
# reading (R5), and there is none here. The weaker one applies: the core answers the
# interface and nothing inside holds it, so whoever calls it is outside (R5b). Answering a
# way in is then enough to make the implementer the application layer (R6), abstract or
# not — what a class does for the hexagon does not wait for it to be instantiable.
status: reviewed
expect: com.example.application.AbstractService = APPLICATION_SERVICE
expect: com.example.ports.in.OrderUseCases = DRIVING_PORT
