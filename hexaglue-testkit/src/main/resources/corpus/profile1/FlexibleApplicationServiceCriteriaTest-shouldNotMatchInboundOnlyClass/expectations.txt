# FlexibleApplicationServiceCriteriaTest#shouldNotMatchInboundOnlyClass
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isFalse()

# Reviewed under the doctrine: the wiring reaches the driven side but stops before the
# edge. A driving port is an interface the core implements AND an adapter calls; this
# fixture has the implementation and no adapter, so the interface stays undecided. That is
# the measured gap, not an oversight of the review.
#
# The implementer consumes no port either, so R6 has nothing to read on it.
status: reviewed
expect: com.example.application.SimpleQueryHandler = UNCLASSIFIED
expect: com.example.ports.in.QueryHandler = UNCLASSIFIED
