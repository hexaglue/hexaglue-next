# FlexibleApplicationServiceCriteriaTest#shouldNotMatchInboundOnlyClass
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isFalse()

# Reviewed under the doctrine: an adapter calling the interface would be the strongest
# reading (R5), and there is none here. The weaker one applies: the core answers the
# interface and nothing inside holds it, so whoever calls it is outside (R5b) — and a
# contract that only answers questions is a way in like any other. Answering it is what
# makes the implementer the application layer (R6), with no port consumed on the far side.
status: reviewed
expect: com.example.application.SimpleQueryHandler = APPLICATION_SERVICE
expect: com.example.ports.in.QueryHandler = DRIVING_PORT
