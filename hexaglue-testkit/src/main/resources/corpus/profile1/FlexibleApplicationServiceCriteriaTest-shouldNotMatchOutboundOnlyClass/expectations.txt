# FlexibleApplicationServiceCriteriaTest#shouldNotMatchOutboundOnlyClass
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isFalse()

# Reviewed under the doctrine: this fixture carries the wiring the rules read, so it states
# what the engine owes once the waves are built. The claims below are what the relations
# say, not what the names suggest.
#
# DELIBERATELY DIFFERENT from the carriere, which required a class to sit on both sides at
# once. R6 asks for a driving port implemented OR a driven port consumed: holding a port
# the core owns and nobody implements is enough to be the application layer.
status: reviewed
expect: com.example.application.BackgroundProcessor = APPLICATION_SERVICE
expect: com.example.ports.out.NotificationGateway = DRIVEN_PORT
