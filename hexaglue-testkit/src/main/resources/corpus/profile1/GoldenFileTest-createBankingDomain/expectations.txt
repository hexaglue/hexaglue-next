# GoldenFileTest#createBankingDomain
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   (no assertion found)

# Reviewed under the doctrine: this fixture carries the wiring the rules read, so it states
# what the engine owes once the waves are built. The claims below are what the relations
# say, not what the names suggest.
#
# Money, Transaction and both ports are declared. Account is the subject of the
# repository's signatures (R1b) and AccountId its search key and its identity field (R2).
status: reviewed
expect: com.example.banking.domain.Account = AGGREGATE_ROOT
expect: com.example.banking.domain.AccountId = IDENTIFIER
expect: com.example.banking.domain.Money = VALUE_OBJECT
expect: com.example.banking.domain.Transaction = ENTITY
expect: com.example.banking.ports.out.AccountRepository = DRIVEN_PORT
expect: com.example.banking.ports.in.TransferUseCase = DRIVING_PORT
