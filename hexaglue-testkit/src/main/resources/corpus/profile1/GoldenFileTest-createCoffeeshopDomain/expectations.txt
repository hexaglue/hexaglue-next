# GoldenFileTest#createCoffeeshopDomain
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   (no assertion found)

# Reviewed under the doctrine: this fixture carries the wiring the rules read, so it states
# what the engine owes once the waves are built. The claims below are what the relations
# say, not what the names suggest.
#
# Both ports and the line item are declared; Location is a value by shape (an enum is a
# closed set of values); Order is the subject of the repository (R1b) and OrderId its
# search key (R2).
status: reviewed
expect: com.coffeeshop.domain.order.Order = AGGREGATE_ROOT
expect: com.coffeeshop.domain.order.OrderId = IDENTIFIER
expect: com.coffeeshop.domain.order.LineItem = VALUE_OBJECT
expect: com.coffeeshop.domain.order.Location = VALUE_OBJECT
expect: com.coffeeshop.ports.out.OrderRepository = DRIVEN_PORT
expect: com.coffeeshop.ports.in.OrderingCoffee = DRIVING_PORT
