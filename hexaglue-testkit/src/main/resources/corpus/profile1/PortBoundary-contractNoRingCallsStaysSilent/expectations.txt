# PortBoundary-contractNoRingCallsStaysSilent
#
# Authored for the boundary of the hexagon. The vocabulary is deliberately
# unconventional: what makes an interface a port is who writes it and who calls
# it, never the word its name ends with or the package it sits in.

# The same core as the scenario above, with the entry point removed. Nothing outside calls
# this contract, so nothing distinguishes a way in from an agreement the core keeps with
# itself, and the engine says neither.
#
# This is the shape most of an inbound port takes in a perimeter reduced to the domain: the
# answer is not wrong, it is the honest consequence of what the sources were asked about.
status: reviewed
expect: com.acme.hangar.core.Assembly = UNCLASSIFIED
expect: com.acme.hangar.core.AssemblyLine = UNCLASSIFIED
