# PortBoundary-contractTheCoreCallsIsADrivenPort
#
# Authored for the boundary of the hexagon. The vocabulary is deliberately
# unconventional: what makes an interface a port is who writes it and who calls
# it, never the word its name ends with or the package it sits in.

# Nothing here declares anything and no framework symbol appears. What makes the interface a
# way out is the position it holds: a type of the core keeps it as a collaborator, and no
# implementation of it lives in the perimeter, so whatever answers those calls is outside.
#
# The caller is the application layer, and the reason is the port and nothing else: it holds
# the one interface the core reaches outward through. Re-arbitrated when the third wave was
# built - until then it read as an undecided value, its single final field being all the
# shape had to say about it.
status: reviewed
expect: com.acme.hangar.core.Ledger = DRIVEN_PORT
expect: com.acme.hangar.core.Checkout = APPLICATION_SERVICE
