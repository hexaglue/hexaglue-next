# PortBoundary-contractTheRingCallsAndTheCoreWritesIsADrivingPort
#
# Authored for the boundary of the hexagon. The vocabulary is deliberately
# unconventional: what makes an interface a port is who writes it and who calls
# it, never the word its name ends with or the package it sits in.

# The mirror of a way out. The core answers this contract and an entry point the framework
# calls holds it, so the contract is how the world reaches in. Both halves are needed, and
# the direction falls out of which side does which - no suffix, no package, no vocabulary.
#
# The type answering the port stays undecided: being the pivot of the application is read by
# the wave after this one.
status: reviewed
expect: com.acme.hangar.web.HangarDoor = DRIVING_ADAPTER
expect: com.acme.hangar.core.Assembly = DRIVING_PORT
expect: com.acme.hangar.core.AssemblyLine = UNCLASSIFIED
