# PortClassifierTest#shouldHandleRecordTypes
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.isUnclassified()).isTrue()

# Reviewed, and DELIBERATELY DIFFERENT from the carriere. The legacy engine returned no
# verdict; here an immutable single-value wrapper called OrderId is read as an identity by
# shape and by name at once. B13's lesson is that a name must not decide alone against
# structure, not that it must be ignored when structure agrees with it.
status: reviewed
expect: com.example.OrderId = IDENTIFIER
