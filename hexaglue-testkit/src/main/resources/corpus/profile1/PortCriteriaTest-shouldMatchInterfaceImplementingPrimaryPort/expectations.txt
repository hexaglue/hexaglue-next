# PortCriteriaTest#shouldMatchInterfaceImplementingPrimaryPort
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(result.matched()).isTrue()
#   assertThat(result.confidence()).isEqualTo(ConfidenceLevel.EXPLICIT)
#   assertThat(result.justification()).contains("PrimaryPort")
#   assertThat(criteria.targetKind()).isEqualTo(PortKind.USE_CASE)
#   assertThat(criteria.targetDirection()).isEqualTo(PortDirection.DRIVING)

# Reviewed under the doctrine, and DELIBERATELY DIFFERENT from the carriere. The fixture
# extends a jMolecules hexagonal port symbol, which jMolecules ships as an annotation and
# not as an interface: no compiler accepts this declaration, so no intent is validly
# declared by it. The legacy engine reached its verdict by matching a simple name on the
# supertypes, which is the naming shortcut v7 removes. A scenario that owes DRIVING_PORT is
# one that annotates the interface, and the corpus already holds it.
status: reviewed
expect: com.example.OrderingCoffee = UNCLASSIFIED
