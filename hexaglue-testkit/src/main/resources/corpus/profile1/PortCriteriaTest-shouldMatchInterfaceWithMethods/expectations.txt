# PortCriteriaTest#shouldMatchInterfaceWithMethods
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(query.methodsOf(useCase)).isNotEmpty()
#   assertThat(result.matched()) .as("Interface with methods should be classified as DRIVING port when implemented by CoreAppClass") .isTrue()
#   assertThat(result.confidence()).isEqualTo(ConfidenceLevel.HIGH)
#   assertThat(criteria.targetDirection()).isEqualTo(PortDirection.DRIVING)

status: draft
# no expectation could be drafted; read the assertions above and decide by hand
