# PortCriteriaTest#shouldNotMatchMarkerInterface
#
# Drafted from the legacy assertions below. The legacy engine is an observation,
# never an oracle: it carries confirmed bugs, and v7 doctrine (D7, D8) deliberately
# changes some verdicts. Read, correct, then set status to reviewed.
#
#   assertThat(markerInterface.isInterface()).isTrue()
#   assertThat(query.methodsOf(markerInterface)).isEmpty()
#   assertThat(markerFacts.implementedByCore()).isTrue()
#   assertThat(result.matched()) .as("Marker interface (no methods) should not be classified as DRIVING port") .isFalse()

status: draft
# no expectation could be drafted; read the assertions above and decide by hand
